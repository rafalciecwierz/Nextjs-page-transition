import FramerDiv from '@/components/Framer/FramerDiv'
import React from 'react'

const AnimateTemplate = ({ children }: { children: React.ReactNode }) => {
    return (
        <>{children}</>
    )
}

export default AnimateTemplate